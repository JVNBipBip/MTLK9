/**
 * Vertical space taken by `components/header.tsx` fixed bar:
 * outer padding `p-4 md:p-6` plus inner nav `h-16 md:h-20`.
 * Update here if header spacing changes.
 */
export const SITE_FIXED_HEADER_MAIN_PT_CLASS = "pt-24 md:pt-32"

/** Sticky page chrome (e.g. booking top bar) aligns under the fixed header. */
export const SITE_FIXED_HEADER_STICKY_TOP_CLASS = "top-24 md:top-32"
